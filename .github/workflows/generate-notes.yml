name: Generate Release Notes

on:
  workflow_dispatch:

jobs:
  generate-notes:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Generate Release Notes
      uses: ./
      env:
        tag: v2
        